apiVersion: v1
kind: Service
metadata:
  name: flask-metrics-app
  namespace: flask-app
  labels:
    app: flask-metrics-app
spec:
  selector:
    app: flask-metrics-app
  ports:
    - name: http
      port: 80
      targetPort: http
